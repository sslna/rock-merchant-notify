# 洛克王国世界远行商人刷新推送实现方案

## 1. 目标

在“远行商人”商品刷新时，自动把本次刷新出的商品列表推送到手机通知栏。

目标效果：

- 到达刷新时间后自动检测商人商品
- 识别出当前商品名称、数量、价格等信息
- 如果和上一次结果不同，则立即推送到手机
- 尽量降低实现复杂度和封号风险

---

## 2. 先说结论

最推荐的实现路线是：

如果**没有稳定接口**：

**PC 端定时执行截图 + OCR 识别商品 + 本地比对变化 + 调用手机推送服务发送通知**

如果**已经有稳定接口**：

**定时请求远行商人接口 + 本地比对变化 + 调用 ntfy 推送手机通知**

有接口时的优点：

- 实现明显更简单
- 准确率通常高于 OCR
- 不依赖固定窗口、分辨率和截图坐标
- 后续更容易扩展成历史记录、订阅商品、Web 面板

无接口时这条路线的优点：

- 不依赖游戏官方接口
- 不需要读取游戏内存，风险相对更低
- 技术门槛适中，适合个人项目
- 后续容易迭代成“截图存档”“刷新历史”“指定商品提醒”

不推荐一开始就做：

- 抓游戏内部接口
- 读取游戏内存
- 注入或自动化点击过深

原因是这类方式开发成本更高，也更容易碰到稳定性和账号风险问题。

---

## 3. 需求拆解

整个需求可以拆成 5 个模块：

1. 定时触发
2. 获取商人当前商品内容
3. 结构化识别商品信息
4. 与上一次结果做比对
5. 推送消息到手机通知栏

对应的数据流如下：

```text
定时器
  -> 打开/切到游戏窗口
  -> 截图远行商人商品区域
  -> OCR/图像识别
  -> 解析成结构化商品列表
  -> 和上次结果比对
  -> 有变化则推送到手机
  -> 保存本次结果
```

---

## 4. 推荐方案架构

## 4.1 整体架构

建议使用 Python 实现，结构如下：

```text
项目目录
├─ main.py                # 主入口
├─ scheduler.py           # 定时任务
├─ capture.py             # 截图/窗口处理
├─ ocr.py                 # OCR 识别
├─ parser.py              # 文本解析为商品结构
├─ comparer.py            # 商品比对
├─ notifier.py            # 手机推送
├─ config.yaml            # 配置文件
├─ data/
│  ├─ latest.json         # 最近一次商品结果
│  ├─ history.jsonl       # 历史记录
│  └─ screenshots/        # 截图存档
└─ logs/
   └─ app.log
```

## 4.2 推荐运行方式

推荐放在你自己的 Windows 电脑上常驻运行：

- 游戏在哪台机器上运行，脚本就放在哪台机器上
- 使用 Windows 任务计划程序，或者脚本内部定时器
- 到刷新前后自动执行检测

如果后续你想做成更稳定的版本，可以拆成：

- 采集端：Windows PC，负责截图和识别
- 服务端：云服务器或本机 HTTP 服务，负责存储和推送

但第一版没必要上服务端，单机足够。

---

## 5. 商品信息怎么获取

这里是整个项目最关键的部分。

## 5.0 API 路线调研结论

截至 **2026-05-25**，我没有查到“洛克王国世界远行商人”的**官方公开 API 文档**，但查到了几类很强的侧面证据，说明社区里已经存在**非官方接口或私有数据源**：

- V2EX 在 **2026-04-29** 的帖子提到：作者先抓包，后又表示“发现有别人做的接口”，并展示了“远行商人原始数据”日志，字段里出现了 `name`、`start_time`、`end_time`
- 第三方站点 `mmclkwg.com` 直接提供“旅行商人实时查看远行商人今日在售商品”，并注明“数据来自 `wiki.lcx.cab`，每 5 分钟更新”
- 好游快爆的远行商人工具页可直接展示当前时段商品、价格、限购与时段信息

这说明：

- **没有找到官方公开接口**
- **大概率存在可调用的非官方接口，或者存在被第三方站点封装过的数据源**

因此当前最优策略是：

1. 先不要继续投入 OCR
2. 优先尝试拿到你现成可用的接口
3. 一旦接口可用，直接改成“轮询接口 + ntfy 推送”

## 5.0.2 已验证的可用页面数据源

已验证下面这个地址当前可访问，并且页面中直接包含远行商人商品文本：

`https://www.onebiji.com/hykb_tools/comm/lkwgmerchant/preview.php?id=1&immgj=0`

验证时间：**2026-05-25**

可直接看到的页面内容包括：

- 商品名
- 价格
- 限购数量
- 时间段选项，例如 `08:00-12:00`、`12:00-16:00`、`16:00-20:00`、`20:00-24:00`

这个数据源的特点是：

- **可用**
- **不是标准 JSON API**
- 更像是一个服务端生成的 HTML 页面
- 可以作为第一版的数据来源

因此技术上可以采用：

`定时请求 HTML 页面 -> 解析商品文本 -> 结构化 -> 比对 -> ntfy 推送`

如果后续你能提供更底层的 JSON 接口，再切过去会更理想。

---

## 5.0.1 如果你提供接口，方案会怎么简化

拿到接口后，项目可以直接简化成下面的数据流：

```text
定时器
  -> 请求远行商人接口
  -> 解析 JSON
  -> 标准化商品列表
  -> 和上次结果比对
  -> 有变化则推送到 ntfy
  -> 保存本次结果
```

对应模块也会简化为：

```text
项目目录
├─ main.py
├─ scheduler.py          # 定时任务
├─ merchant_api.py       # 请求接口
├─ normalizer.py         # 结构标准化
├─ comparer.py           # 商品比对
├─ notifier_ntfy.py      # 安卓 ntfy 推送
├─ config.yaml
└─ data/
   ├─ latest.json
   └─ history.jsonl
```

这个版本会比 OCR 版本更值得优先开发。

## 5.1 方案 A：OCR 识别界面文字

这是最推荐的方案。

做法：

- 在刷新时间点，脚本自动截取远行商人商品区域
- 对截图做裁切、放大、灰度化、二值化等预处理
- 用 OCR 识别商品名称、价格、数量
- 再把 OCR 文本解析成结构化数据

优点：

- 最稳
- 最通用
- 不依赖游戏内部实现

缺点：

- OCR 准确率需要调试
- 游戏 UI 变化后可能要重新适配

推荐工具：

- `mss`：截图
- `opencv-python`：图像预处理
- `paddleocr`：中文 OCR，准确率通常比纯 Tesseract 更好

适用前提：

- 商人商品区域位置固定，或者至少界面布局稳定

## 5.2 方案 B：模板匹配识别商品图标

如果商品名称 OCR 不稳定，可以加一个图标识别兜底。

做法：

- 为常见商品准备图标模板库
- 对商品格子做模板匹配
- 匹配到图标后，辅助确认商品名称

优点：

- 对部分短文字、花体字、低清字更稳

缺点：

- 需要自己维护模板库
- 新商品出现时要补模板

推荐用法：

- **OCR 为主，图标匹配为辅**

## 5.3 方案 C：抓包/接口分析

理论上如果游戏客户端和服务端通信里包含商人商品数据，可以通过抓包拿到。

但问题很多：

- 不确定是否加密
- 不确定数据是否容易定位
- 成本较高
- 风险高于截图 OCR

建议：

- 第一版不要走这条路

## 5.4 方案 D：读取内存

不推荐。

原因：

- 开发难度更高
- 抗更新能力差
- 风险更高

---

## 6. 推送到手机通知栏怎么做

你真正需要的是一个“从脚本发 HTTP 请求，就能在手机出现通知”的服务。

下面几种都可以：

## 6.1 Bark

适合 iPhone。

原理：

- 手机安装 Bark
- 获得一个推送地址
- 脚本通过 HTTP 请求发送标题和内容
- iPhone 通知栏直接收到消息

优点：

- 配置简单
- 很适合个人项目

## 6.2 ntfy

适合 Android，也适合自建。

原理：

- 手机安装 ntfy App
- 订阅一个主题
- 脚本向该主题发消息
- 手机收到通知

优点：

- 简单
- 跨平台
- 可自建

## 6.3 PushPlus / Server酱 / 企业微信机器人

也都能实现“脚本推送到手机”。

优点：

- 国内网络环境下通常更容易用

缺点：

- 各平台的额度、稳定性、配置方式不同

## 6.4 推荐结论

如果你是：

- iPhone：优先 `Bark`
- Android：优先 `ntfy`
- 想要国内方案：可以考虑 `PushPlus`

你当前已经确定是 **Android + ntfy**，所以后续实现可以直接围绕 `ntfy` 写，不需要再保留 Bark 相关逻辑。

---

## 7. 推荐的第一版实现方式

我建议第一版按下面方式做：

### 方案名称

如果暂时没有接口：

**固定时间截图识别 + ntfy 推送**

如果你能提供接口：

**固定时间轮询接口 + ntfy 推送**

### 执行流程

1. 提前配置远行商人每日刷新时间
2. 在刷新时间前后 1 到 3 分钟内，每隔 10 到 30 秒请求一次接口
3. 将接口结果整理为统一 JSON
4. 与 `latest.json` 做比对
5. 如果变化了，则发送 `ntfy` 手机通知
6. 保存本次结果

### 为什么这样做

- 避免只检测一次导致错过
- 刷新时间附近多次检测，容错更高
- 先只做“检测到变化就推送”，实现最快

---

## 8. 数据结构建议

建议统一保存成这样的结构：

```json
{
  "npc": "远行商人",
  "captured_at": "2026-05-25T20:00:15+08:00",
  "items": [
    {
      "name": "高级咕噜球",
      "price": 1200,
      "count": 3,
      "slot": 1
    },
    {
      "name": "可可果",
      "price": 800,
      "count": 5,
      "slot": 2
    }
  ],
  "source": "merchant_api",
  "raw": {
    "request_time": "2026-05-25T20:00:15+08:00"
  }
}
```

对比时建议只比较这些关键字段：

- 商品名称
- 数量
- 价格
- 格子顺序

如果这几个字段任意变化，就认为商店刷新了。

---

## 9. OCR 识别的关键难点

这个项目能不能好用，主要取决于 OCR 稳定性。

## 9.1 难点

- 商品名可能有描边、阴影、特效字
- 背景复杂时识别容易错
- 不同分辨率下坐标变化
- 数字和中文混排可能识别不稳

## 9.2 应对策略

建议按下面顺序处理：

1. 固定游戏分辨率和窗口位置
2. 只裁剪商品区域，不要整屏 OCR
3. 对每个商品格子单独截图识别
4. 先做图像预处理：
   - 灰度化
   - 放大 2 倍
   - 锐化
   - 二值化
5. 对商品名、价格、数量分区域识别
6. 加“词典纠错”

例如：

- OCR 识别成“可可杲”，自动纠正为“可可果”
- OCR 识别成“高汲咕噜球”，自动纠正为“高级咕噜球”

这一步非常有用。

## 9.3 词典纠错建议

维护一个已知商品字典：

```text
高级咕噜球
咕噜球
可可果
魔力化肥
经验果实
......
```

识别后用“相似度匹配”纠正到最接近的合法商品名。

推荐库：

- `rapidfuzz`

---

## 10. 手机推送内容建议

通知内容不要太长，但要一眼看清。

推荐格式：

```text
远行商人已刷新
1. 高级咕噜球 x3 - 1200
2. 可可果 x5 - 800
3. 经验果实 x2 - 1500
```

如果只关心某些商品，还可以加“重点提醒”：

```text
远行商人已刷新
发现目标商品：高级咕噜球
当前商品：
1. 高级咕噜球 x3 - 1200
2. 可可果 x5 - 800
```

---

## 11. 配置文件建议

建议用 `config.yaml`：

```yaml
merchant:
  name: "远行商人"
  refresh_times:
    - "08:00"
    - "12:00"
    - "16:00"
    - "20:00"
  detect_before_seconds: 60
  detect_after_seconds: 180
  detect_interval_seconds: 15

notify:
  provider: "ntfy"
  ntfy:
    topic: "rock-merchant"
    server: "https://ntfy.sh"

api:
  url: "https://your-api.example.com/merchant"
  method: "GET"
  timeout_seconds: 10
  headers:
    Authorization: "Bearer your_token"
  items_path: "data.items"
  name_field: "name"
  price_field: "price"
  count_field: "count"
  slot_field: "slot"
  start_time_field: "start_time"
  end_time_field: "end_time"

filters:
  watch_items:
    - "高级咕噜球"
    - "可可果"
```

注意：

- `refresh_times` 需要你根据游戏实际刷新时间填写
- `api` 字段需要根据你最终提供的接口格式调整

---

## 12. 最小可行版本（MVP）

先做一个最小版本，不要一上来追求全自动。

### MVP 功能

- 运行一次脚本
- 脚本请求远行商人接口
- 输出 JSON
- 推送到手机

### 第二步

- 加定时执行
- 加刷新时间窗口轮询
- 加变化比对

### 第三步

- 指定商品重点提醒
- 失败重试
- 多个 ntfy 订阅主题

这样做的好处是：

- 先验证接口是否稳定
- 先验证手机推送链路通不通
- 避免一开始把 OCR 和接口问题混在一起

---

## 13. 风险与限制

## 13.1 最大风险

如果你拿到的是第三方接口，最大风险会变成：

- 接口失效
- 接口字段变化
- 限流
- 鉴权过期
- 上游返回名称不规范

解决方式：

- 对接口响应做兼容解析
- 本地保存原始响应，便于排错
- 加重试与错误告警
- 尽量不要把字段名写死在业务逻辑里

## 13.2 如果接口被限流或偶发失败

脚本即使触发了，也可能拿不到本次结果。

解决方式有三种：

1. 在刷新时间窗口内多次轮询
2. 给每次请求设置超时和重试
3. 对同一时间段结果做去重
第一版建议先做第 1 种和第 2 种。

## 13.3 商品名可能不规范

这在第三方接口里比较常见。

解决方式：

- 做商品名标准化映射
- 维护别名表
- 用相似度匹配做纠错

这样会稳很多。

## 13.4 合规和账号风险

建议优先使用：

- 截图
- OCR
- 本地通知推送

尽量避免：

- 内存读取
- 注入
- 深度自动化操作

---

## 14. 推荐技术栈

推荐 Python：

- `python 3.11+`
- `rapidfuzz`：商品名纠错
- `pyyaml`：配置读取
- `requests`：推送通知
- `loguru` 或 `logging`：日志

可选：

- `schedule`：轻量定时任务
- `tenacity`：请求重试

---

## 15. 推荐开发顺序

建议按下面顺序做，成功率最高：

1. 先确认远行商人刷新时间
2. 拿到可用接口示例
3. 先写接口请求脚本
4. 把接口结果整理成统一商品列表 JSON
5. 接入 `ntfy` 推送
6. 加本地结果对比
7. 再接入定时器
8. 最后再补异常重试和重点商品提醒

---

## 16. 我建议你采用的最终路线

如果你希望项目尽快做出来，我建议采用下面这个版本：

### 第一阶段

**脚本定时请求接口 + 手机 ntfy 推送**

这是现在最现实、最容易成功的版本。

### 第二阶段

**增加重试、去重、商品别名映射**

提升稳定性。

### 第三阶段

**加入目标商品提醒、历史记录、简单 Web 面板**

提升实用性。

---

## 17. 后续如果要继续开发

下一步最值得做的不是直接写全套系统，而是先完成下面 3 件事：

1. 提供一个可用的远行商人接口示例
2. 确认每日刷新时间
3. 确认接口返回字段：
   - 商品名字段
   - 价格字段
   - 数量字段
   - 时间字段

只要这 3 件事明确，就可以马上开始落第一版代码。

---

## 18. 一句话总结

这个需求最稳的实现方式是：

**优先使用远行商人接口，在刷新时段定时请求数据，和上次结果比对后，通过 ntfy 推送到安卓通知栏；只有在没有可用接口时，才退回到截图 OCR 方案。**

这条路线实现成本低、风险相对小，也最适合个人长期维护。
